# Server Configuration
server.port=8081

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/online_book_store?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Vamsi@7075
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false

# Performance Optimizations
spring.jpa.properties.hibernate.jdbc.batch_size=25
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true
spring.jpa.open-in-view=false

# Connection Pool Optimizations
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# JWT Configuration
jwt.secret=ThisIsAReallyLongSecretKeyForJWTTokenGenerationThatShouldBeAtLeast64BytesLongAndSecure
jwt.expiration=86400000

# Alternative Mail Configuration using SendGrid (more reliable for transactional emails)
# Uncomment the SendGrid configuration and comment out Gmail if Gmail continues to fail

# SendGrid Configuration (RECOMMENDED for production - better deliverability)
# spring.mail.host=smtp.sendgrid.net
# spring.mail.port=587
# spring.mail.username=apikey
# spring.mail.password=YOUR_SENDGRID_API_KEY_HERE
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# For Gmail to work properly with transactional emails, ensure:
# 1. You have enabled 2-factor authentication on your Google account
# 2. You have generated an App Password specifically for this application
# 3. Your IP address is not blacklisted
# 4. You're not exceeding Gmail's sending limits (100/day for free accounts)

# If using Gmail, you may need to enable access for less secure apps or use OAuth2

# Gmail Configuration (Primary)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=officialvamsi77@gmail.com
spring.mail.password=brqh tyar cvqf evga
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# Additional properties to improve deliverability
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000
spring.mail.properties.mail.smtp.quitwait=false
spring.mail.properties.mail.smtp.userset=true
spring.mail.properties.mail.smtp.ehlo=true

# Gmail Configuration (Alternative - uncomment if needed)
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=officialvamsi77@gmail.com
# spring.mail.password=brqhtyarcvqfevga
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=true
spring.mail.test-connection=false

# Logging
logging.level.com.bookstore=DEBUG
logging.level.org.springframework.mail=DEBUG
logging.level.jakarta.mail=DEBUG
logging.level.org.apache.hc.client=DEBUG
logging.level.org.apache.http=DEBUG